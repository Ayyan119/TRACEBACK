# KB-1001: Transcoding Temporary Workspace
Temporary FFmpeg workspace is used for intermediate files during transcoding.
If the workspace reaches capacity, FFmpeg jobs can fail with ENOSPC. Failed jobs can increase the transcoding queue and prevent manifests from being generated.
A deployment that reduces workspace capacity can be a contributing cause when disk saturation and ENOSPC occur after the deployment.
